opencv-python>=4.8.0
numpy>=1.24.0
pyautogui>=0.9.54
Pillow>=10.0.0
websockets>=12.0
# MediaPipe — Google's face mesh model (gaze + blink + head-pose detection)
# Falls back to Haar cascades automatically if not installed
mediapipe>=0.10.0
